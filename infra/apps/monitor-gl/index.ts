import { AppModule } from '../app-module'
import { MonitorGlConfig as Config } from './monitor-gl-config'
import { MonitorGlAppStack as AppStack } from './monitor-gl-app-stack'

export default {
  AppStack,
  Config,
} as AppModule
